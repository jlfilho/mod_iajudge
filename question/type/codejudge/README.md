# qtype_codejudge

Coding question type scaffold for `iajudge`.

This plugin is being introduced in phase 1 as the base structure for a Moodle question type
that will be used by the `iajudge` activity as the only accepted question type.
